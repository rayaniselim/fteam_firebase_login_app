library core_module;

/// A Calculator.
class Calculator {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;
}

/// o core da app sao coisas que se reaproveita em todo o codigo
/// no caso aqui vem o textFormField e as dependencias
/// 